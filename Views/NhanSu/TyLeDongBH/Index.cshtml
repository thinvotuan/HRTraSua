@using BatDongSan.Models.NhanSu
@model IList<tbl_NS_TyLeDongBH>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int i = 1;    
}
<link rel="stylesheet" href="~/Content/batdongsan-alert.css" />
<script src="~/Scripts/batdongsan.generals.js"></script>
<style>
    #content td, th
    {
        border-bottom: 1px inset #ADC2D6;
        position: relative;
        text-align: center;
    }
</style>
<script type="text/javascript">
    $(document).ready(function () {
        $("#btnModalCreate").click(function () {
            var url = "@Url.Action("Create", "TyLeDongBH")";
            var form = document.getElementById("form-submit");
            form.action = url;
            $("#custom-panel").load(url)
        })

        $("#customModal").on("hide.bs.modal", function () {
            $("#custom-panel").html("");
        });

        $(document).on("click", ".btnDelete", function () {
            var url = "@Url.Action("Delete", "TyLeDongBH")" + "/" + this.value;
            bootbox.confirm("Bạn có muốn xóa không", function (result) {
                if (result) {
                    var form = document.getElementById("form-listview");
                    form.method = "post";
                    form.action = url;
                    form.submit();
                }
            });
        })

    });

</script>
<form id="form-listview">
    <div id="container">
        <fieldset class="removeBorderFieldset">
            <div id="tabs" class="ui-tabs">
                <ul class="ui-tabs-nav">
                     <li class="ui-tabs-selected ui-state-active"> <a><span><i class="fa fa-list" aria-hidden="true"> Tỷ lệ đóng bảo hiểm</i></span></a></li>
                </ul>
            </div>
            <div class="panel-body background_top">
                <div class="form-inline">
                    <div class="form-group" style="float: right">
                        <button class="btn btn-info" id="btnModalCreate" type="button" data-toggle="modal" data-target="#customModal">Thêm </button>
                    </div>
                </div>
            </div>
            <div id="content">
                <table class="table table-striped table-hover table-condensed" id="maintable">
                    <thead>
                        <tr>
                            <th rowspan="2" style="text-align: center; width: 45px; border-right: 1px solid #DADEE4; border-left: 1px solid #DADEE4">STT</th>
                            <th colspan="2" style="border-right: 1px solid #DADEE4; border-left: 1px solid #DADEE4">BHXH</th>
                            <th colspan="2" style="border-right: 1px solid #DADEE4; border-left: 1px solid #DADEE4">BHYT</th>
                            <th colspan="2" style="border-right: 1px solid #DADEE4; border-left: 1px solid #DADEE4">BHTN</th>
                            <th rowspan="2" style="border-right: 1px solid #DADEE4; border-left: 1px solid #DADEE4">Ngày áp dụng</th>
                            <th rowspan="2" style="border-right: 1px solid #DADEE4; border-left: 1px solid #DADEE4">Ghi chú</th>
                        </tr>
                        <tr>
                            <th>% Doanh nghiệp đóng</th>
                            <th style="border-right: 1px solid #DADEE4;">% Nhân viên đóng</th>
                            <th>% Doanh nghiệp đóng</th>
                            <th style="border-right: 1px solid #DADEE4;">% Nhân viên đóng</th>
                            <th>% Doanh nghiệp đóng</th>
                            <th>% Nhân viên đóng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {                            
                            <tr>
                                <td style="text-align: center">@i</td>
                                <td style="text-align: center">@item.baoHiemXHDoanhNghiep</td>
                                <td style="text-align: center">@item.baoHiemXHNhanVien</td>
                                <td style="text-align: center">@item.baoHiemYTeDoanhNghiep</td>
                                <td style="text-align: center">@item.baoHiemYTeNhanVien</td>
                                <td style="text-align: center">@item.baoHiemTNDoanhNghiep</td>
                                <td style="text-align: center">@item.baoHiemTNNhanVien</td>
                                <td style="text-align: left">@item.ngayApDung.Value.ToString("dd/MM/yyyy")</td>
                                <td style="text-align: left">@item.ghiChu</td>

                                @*<td style="text-align: right">
                                    <button class="btn btn-primary btn-xs btnEdit" type="button" data-toggle="modal" data-target="#customModal" value="@item.id">Sửa</button>
                                    <button class="btn btn-warning btn-xs btnDelete" type="button" value="@item.id">Xóa</button>
                                </td>*@
                            </tr> 
                            i++;
                        }
                    </tbody>
                </table>
            </div>
        </fieldset>
    </div>
</form>
<form id="form-submit" method="post">
    <!--------------------------- Modal - Thêm Sửa Modal Form ------------------------>
    <div id="customModal" class="modal fade" role="dialog">
        <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title"></h4>
                  </div>
                <div class="modal-body">
                    <div class="container" style="width: 100%">
                        <div class="row">
                            <div id="custom-panel" class="filter-panel">
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-primary" id="btnSubmit">Lưu</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal" id="btnCloseEmail">Đóng</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
